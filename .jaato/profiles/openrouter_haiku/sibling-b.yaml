# sibling-b — openrouter set: anthropic/claude-haiku-4.5.
name: sibling-b
inherits: [_base_sibling-b]
plugins: []          # empty keeps the inherited _base surface
model: anthropic/claude-haiku-4.5
provider: openrouter
plugin_configs:
  openrouter:
    api_key: "${JAATO_OPENROUTER_API_KEY}"
    api_params:
      temperature: 0.0   # determinism knob
      max_tokens: 512
